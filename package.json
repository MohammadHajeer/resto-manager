{
  "name": "resto-manager",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared/validators"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build:validators": "npm run build --workspace=shared/validators",
    "install:all": "npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
